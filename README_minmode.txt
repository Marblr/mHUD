The 'cl_hud_minmode' command is used to toggle between a 12-player scoreboard and a 6-player scoreboard.

To use an actual minmode version of mHUD, bring the contents of mHUD_minmode to the root HUD folder and overwrite the files.